
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "definitions.h"



LeNet* create_lenet(void) {


    LeNet* net = (LeNet*)malloc(sizeof(LeNet));
    if (!net) return NULL;

    net->conv1.weights = (float*)malloc()

}